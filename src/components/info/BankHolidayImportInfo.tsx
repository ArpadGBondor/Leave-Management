import InfoBubble from './InfoBubble';

export default function BankHolidayImportInfo() {
  return (
    <InfoBubble>
      The system was initially populated with several years of UK bank holiday
      data. Future bank holiday information will need to be imported as it
      becomes available on GOV.UK.
    </InfoBubble>
  );
}
