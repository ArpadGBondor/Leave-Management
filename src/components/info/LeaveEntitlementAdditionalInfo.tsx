import InfoBubble from './InfoBubble';

export default function LeaveEntitlementAdditionalInfo() {
  return (
    <InfoBubble>
      <span className="font-bold">Additional leave entitlement</span>: Some
      employees receive extra holiday on top of the base allowance — such as
      contractual enhancements, long-service awards, or other company benefits.
      These days are added to the base entitlement before calculating the final
      total.
    </InfoBubble>
  );
}
